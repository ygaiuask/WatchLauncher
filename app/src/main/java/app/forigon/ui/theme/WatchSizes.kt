package app.forigon.ui.theme

import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

/**
 * Sizes optimized for ~1.4" watch screens (320-400px)
 */
object WatchSizes {
    // Bubble grid items
    val bubbleSize = 56.dp
    val bubbleIconSize = 36.dp
    val bubbleLabelSize = 9.sp

    // List items
    val listItemHeight = 48.dp
    val listIconSize = 32.dp

    // Touch targets (minimum 48dp for accessibility)
    val minTouchTarget = 48.dp

    // Typography for watch
    val timeDisplaySize = 48.sp
    val titleSize = 16.sp
    val bodySize = 12.sp
    val captionSize = 10.sp

    // Spacing
    val screenPadding = 8.dp
    val itemSpacing = 4.dp
}